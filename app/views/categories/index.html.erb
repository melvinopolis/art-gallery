<div class="container-fluid">
        <% @categories.each do |category| %>
                <div class="header"><h1><%= link_to category.name, category %></h1></div>

                <div class="row">
                    <div class="col-xs-6 col-s-4 col-lg-2">
                    
                        
                    </div>
                </div>
        <% end %>
</div>
    