<div class="container-fluid">
	<h1 class="header">Members</h1>
	<div class="row">
		
			<ul class="list-inline members" >	
				<% @users.each do |user| %>
					<% if user.avatar? == true %>
						<li class="col-xs-4 col-sm-3 col-m-2"><%= link_to image_tag(user.avatar_url, class: "img-responsive"), profile_path(user.id) %><br>
						<p><%= user.username %></li></p>
					<% else %>
						<li class="col-xs-4 col-sm-3 col-m-2"><%= link_to image_tag("gllry.png", class: "img-responsive"), profile_path(user.id) %><br>
						<p><%= user.username %></li></p>
					<% end %>
				<% end %>
				<!-- Need to figure how to make them not stacked... -->
			</ul>
		</div>
	</div>
