<div class="container-fluid">
  	<h1 class="header">Browse All Art</h1>
  		<div class="row">
    		<% @pictures.each do |picture| %>
				<div class="col-xs-6 col-s-4 col-lg-2" id="pic-site">
					<%= link_to image_tag(picture.image_url), picture_path(picture.id)%>
    			</div>
    		<% end %>
  		</div>
</div>